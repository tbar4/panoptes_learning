# Part VI — Hardening
# (auto-converted from MCQ; some upgraded to ShortAnswer/Tracing by hand)

[[questions]]
type = "MultipleChoice"
prompt.prompt = "The Stage-4 reliability step is written as a README contract rather than Rust code. What is the reasoning?"
prompt.distractors = [
  "The reliability step is entirely optional, so a short prose note is enough and it may safely be skipped",
  "Rust has no real way to read a CSV file at all, so the reliability computation simply cannot ever live inside the harness itself",
  "It really should be Rust; writing it as a README is just a shortcut that was taken in order to save some effort",
]
answer.answer = "Reliability stats belong in Python where the libraries are mature, and the README makes the Rust-to-Python contract explicit"
context = """The boundary is a deliberate right-tool decision, and documenting it as a contract (what Python reads, what it produces) keeps the handoff explicit rather than implicit. The Rust side stops at producing the validated files."""
id = "c561bd2a-56fc-4288-aa8e-8ae9f42208c3"

[[questions]]
type = "MultipleChoice"
prompt.prompt = "Which set of invariants does the workspace README enshrine?"
prompt.distractors = [
  "Compile speed, memory footprint, thread-safety, and zero-cost abstraction guarantees",
  "That the full test suite passes and that every public item carries a doc comment",
  "The pinned model list, the API base URL, and the random seed used for shuffling",
]
answer.answer = "Append-only log, blinded sheets, enums-as-validation, and manifest-as-join-spine"
context = """Those four are the load-bearing correctness properties built across the course: the log only grows, sheets cannot leak identity, coded values cannot leave the codebook, and the manifest ties everything together for analysis. The README states them so they survive future edits."""
id = "4822cc83-ad85-4f5b-bef6-3a55ece4b20d"
